{
  "permissions": {
    "allow": ["Bash(pnpm *)", "Bash(git *)", "Write(*)", "Edit(*)"]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path // \"\"'); case \"$file\" in \"$CLAUDE_PROJECT_DIR\"/*.vue|\"$CLAUDE_PROJECT_DIR\"/*.ts|\"$CLAUDE_PROJECT_DIR\"/*.js) pnpm --dir \"$CLAUDE_PROJECT_DIR\" run format ;; esac",
            "statusMessage": "Formatting..."
          },
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path // \"\"'); case \"$file\" in \"$CLAUDE_PROJECT_DIR\"/*.vue|\"$CLAUDE_PROJECT_DIR\"/*.ts|\"$CLAUDE_PROJECT_DIR\"/*.js) pnpm --dir \"$CLAUDE_PROJECT_DIR\" run lint ;; esac",
            "statusMessage": "Linting..."
          }
        ]
      }
    ]
  }
}
